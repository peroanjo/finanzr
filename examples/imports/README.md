# Synthetic importer examples

Every file in this directory was written specifically for the public project.
It does not derive from a broker export or a real portfolio. Values prefixed
with `DEMO-` are intentionally fictitious; public instrument identifiers are
used only to exercise the parser contracts.

The examples cover the three currently supported formats. They are small
enough to inspect manually before uploading them to a local demo installation.
